def parrot(squawk="Squawk!"):
    print(squawk)
#  this will clear first two pytest errors, prints argument and returns passed argument  

# adding the return clears the last two printing Squawk and returning Squawk if no argument is passed 
    return squawk